collections_config:
  pages:
    _enabled_editors:
      - visual
      - content
      - source
  ru:
    add_options:
      - name: New test file
        schema: test
    schemas:
      test:
        path: schemas/test.md