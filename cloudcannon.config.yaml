collections_config:
  careers:
    add_options:
      - name: Add New
        schema: default
  ru:
    add_options:
      - name: Добавить вакансию внутри career
        collection: ru/careers
        schema: default