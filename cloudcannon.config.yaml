collections_config:
  pages:
    _enabled_editors:
      - visual
      - content
      - source
  en:
    add_options:
      - name: New file
        schema: new
    schemas:
      new:
        path: schemas/schema_en.md
  ru:
    singular_name: Вакансия
    add_options:
      - name: New test file
        schema: test
    schemas:
      test:
        path: schemas/test_ru.md
_inputs:
  category:
    type: select
    options:
      values: data.options
      value_key: key
      text_key: key

data_config: true

